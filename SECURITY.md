# Security Policy

## 敏感数据

本项目会处理视频、现场声音轨、AI 配音和第三方 API Key。请勿提交以下内容：

- `.env` 或任何真实 API Key、访问令牌、供应商 voice ID
- `.data/` 中的用户原片、分析副本、字幕、音轨和生成成片
- 未经公开发布授权的真人录音或克隆音色试听文件

仓库提供的 `.gitignore` 已排除这些默认路径，但发布前仍应检查 `git status` 和 `git diff --cached`。

## 报告问题

发现密钥泄漏、任意文件读取、未授权任务访问或其他安全问题时，请通过 GitHub 仓库的私密安全报告功能联系维护者，不要在公开 Issue 中粘贴密钥、用户视频或个人声音样本。

## 密钥泄漏处理

一旦真实密钥进入提交、日志或截图，请先在对应供应商后台撤销并轮换密钥，再清理 Git 历史和相关构建产物。
